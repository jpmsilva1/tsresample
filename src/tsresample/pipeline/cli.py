"""Console script ``tsresample`` (Layer 3)."""

import sys


def main() -> int:
    """Entry point; the command surface is built in node 10."""
    print(
        "tsresample: the command-line interface is not implemented yet.",
        file=sys.stderr,
    )
    return 2
